name: Bake JSON
on:
  push:
    paths:
      - 'data/**/*.json'
      - 'build.js'
      - '**/*.html'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Bake
        run: node build.js
      - name: Commit
        run: |
          git config user.name "dzvt-bot"
          git config user.email "bot@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "chore: bake [skip ci]"
          git push
